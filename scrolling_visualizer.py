#%matplotlib tk

#scrolling visualizer 
#fig, ax = plt.subplots()
#for x in range(raw_train_X.shape[0]):
#    ax.clear()
#    ax.imshow([i for i in 255 - raw_train_X[x].T], cmap='gray') #all data is flipped over, so we use T for transpose
#    title = 'label %d = %s' %(raw_train_y[x],labels[raw_train_y[x]])
#    ax.set_title(title,fontsize=20)
#    plt.pause(1)
